# «remember to put a reference to the data source here»

OPERATOR:

TURBINE:
  make: 
  model: 
  power_MW: 

# This is relevant only for plotting. By default, the `gplot()` function will
# rotate the site by this angle (degrees). Use `gplot(..., landscape=False)`
# to have the site plotted in its original orientation.
LANDSCAPE_ANGLE: 

# The fields below must be lists of tagged coordinate pairs. This requires a
# very specific string format (the number of decimal places is flexible):
#   TAG 11°22.333'N 44°55.666'E

EXTENTS: |-

SUBSTATIONS: |-

TURBINES: |-
